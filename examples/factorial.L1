l -> -1000, l2 -> -1000, l3 -> -1000, l1 -> 5
l := 1;
while !l1 >= 1 do (
    l2 := !l1;
    l3 := !l;
    while !l2 >= 2 do (
        l := !l + !l3;
        l2 := !l2 + -1
    );
    l1 := !l1 + -1
);
!l
